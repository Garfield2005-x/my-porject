export default function product(){
    return(
        <main>
            <section className="hero" style={{height: "50vh"}}></section>

            <section className="search">
                <button>detail</button>
            </section>

            <section className="hotdeal"></section>
        </main>
    );
}